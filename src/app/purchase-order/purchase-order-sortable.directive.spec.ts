import { PurchaseOrderSortableDirective } from './purchase-order-sortable.directive';

describe('PurchaseOrderSortableDirective', () => {
  it('should create an instance', () => {
    const directive = new PurchaseOrderSortableDirective();
    expect(directive).toBeTruthy();
  });
});
