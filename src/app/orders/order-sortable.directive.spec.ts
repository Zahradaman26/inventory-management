import { OrderSortableHeader } from './order-sortable.directive';

describe('OrderSortableDirective', () => {
  it('should create an instance', () => {
    const directive = new OrderSortableHeader();
    expect(directive).toBeTruthy();
  });
});
