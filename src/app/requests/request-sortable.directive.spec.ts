import { RequestSortableDirective } from './request-sortable.directive';

describe('RequestSortableDirective', () => {
  it('should create an instance', () => {
    const directive = new RequestSortableDirective();
    expect(directive).toBeTruthy();
  });
});
