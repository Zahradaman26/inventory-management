import { UserSortableDirective } from './user-sortable.directive';

describe('UserSortableDirective', () => {
  it('should create an instance', () => {
    const directive = new UserSortableDirective();
    expect(directive).toBeTruthy();
  });
});
