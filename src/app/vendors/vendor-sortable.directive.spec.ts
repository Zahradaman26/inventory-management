import { VendorSortableDirective } from './vendor-sortable.directive';

describe('VendorSortableDirective', () => {
  it('should create an instance', () => {
    const directive = new VendorSortableDirective();
    expect(directive).toBeTruthy();
  });
});
